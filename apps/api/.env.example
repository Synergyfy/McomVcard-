# ──────────────────────────────────────────────
# MCOM API — Environment Variables
# Copy to .env and fill in values before running.
# ──────────────────────────────────────────────

# Application
NODE_ENV=development          # development | production | test
PORT=3001                     # API listen port

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=postgres
DB_NAME=mcomvcard

# Authentication
JWT_SECRET=                   # REQUIRED — change to a long random string in production
JWT_EXPIRES_IN=1h             # Access token lifetime
REFRESH_TOKEN_EXPIRES_IN=7d   # Refresh token cookie lifetime

# Public URLs (used in email links, CORS, Swagger)
API_PUBLIC_URL=http://localhost:3001/api
WEB_PUBLIC_URL=http://localhost:3000

# Mail (SMTP) — optional, needed for email verification / notifications
MAIL_HOST=
MAIL_PORT=
MAIL_USER=
MAIL_PASS=
MAIL_FROM=
MAIL_SECURE=false             # "true" for port 465, "false" for 587

# CORS — comma-separated origins for production (e.g. "https://app.example.com,https://www.example.com")
CORS_ORIGINS=
