PORT=3001
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=postgres
DB_NAME=mcomvcard
JWT_SECRET=dev-secret
# For safety, keep TYPEORM_SYNC disabled by default. Set to 'true' only for local dev when
# you need schema auto-sync. NEVER enable in production.
TYPEORM_SYNC=false
