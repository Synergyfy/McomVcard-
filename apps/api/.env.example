# ===== Server =====
NODE_ENV=development
PORT=3001
API_PUBLIC_URL=http://localhost:3001/api
WEB_PUBLIC_URL=http://localhost:3000

# ===== Database (local Postgres) =====
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=change-me
DB_NAME=mcom-vcards
TYPEORM_SYNC=false

# ===== Auth =====
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=1h
REFRESH_TOKEN_EXPIRES_IN=7d

# ===== CORS (comma-separated origins for production) =====
CORS_ORIGINS=

# ===== Mail (optional) =====
MAIL_HOST=
MAIL_PORT=
MAIL_USER=
MAIL_PASS=
MAIL_FROM=
MAIL_SECURE=false

# ===== MCOM Solutions Central Hub (SSO + Billing) =====
MCOM_SOLUTIONS_URL=http://localhost:3010

# Application OAuth 2.0 Credentials
MCOM_CLIENT_ID=mcom-vcard
MCOM_CLIENT_SECRET=change-me

# Server-to-Server Inter-Service Security
# Inbound System API Key — authorizes MCOM Solutions to call /api/v1/system/*
MCOM_SOLUTION_API_KEY=
MCOM_HMAC_SECRET=change-me

# Webhook Verification Secret — HMAC key for inbound lifecycle webhooks
# (header: X-Mcom-Webhook-Signature: sha256=<hex> over the raw body)
MCOM_WEBHOOK_SECRET=

# Shared JWT SSO Secret — verifies the Direct Dashboard Handshake
# (/sso-login?token=...). Signed by MCOM Central with issuer `mcom-central`.
SSO_SECRET=change-me

# Configured App Settings
MCOM_PLATFORM_SLUG=vcard
MCOM_REDIRECT_URI=http://localhost:8000/auth/callback
MCOM_SCOPES=profile email business membership packages
MCOM_MEMBERSHIP_URL=http://localhost:3000

# Centralized MCOM Wallet partner integration (proxy /wallet to MCOM Solutions)
# true  → balance/transactions/writes go through /api/v1/wallet/partner
# false → fall back to the local Postgres wallet tables
MCOM_WALLET_ENABLED=true